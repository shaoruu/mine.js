import { ApolloLink } from 'apollo-link';
import { HttpOptions } from './types';
export declare class HTTPLinkDataloader extends ApolloLink {
    constructor(options: HttpOptions);
    private static createBatchedHttpRequest(options);
}
